# Restrict this browser key to Maps JavaScript API + Places API and your allowed origins.
VITE_GOOGLE_MAPS_API_KEY=replace_with_restricted_browser_key

# Optional cloud map style ID. Without it, the launcher uses its local warm cartographic style.
VITE_GOOGLE_MAPS_MAP_ID=
